package com.raved.content.controller;

import org.springframework.web.bind.annotation.*;

/**
 * REST Controller for tag operations
 */
@RestController
@RequestMapping("/api/tags")
@CrossOrigin(origins = "*")
public class TagController {
    // Tag controller implementation
}
