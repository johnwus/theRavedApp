package com.raved.ecommerce.model;

/**
 * Payment status enum for TheRavedApp
 */
public enum PaymentStatus {
    PENDING,
    SUCCESS,
    FAILED,
    CANCELLED,
    REFUNDED,
    REFUND_REQUESTED,
    PROCESSING
}
