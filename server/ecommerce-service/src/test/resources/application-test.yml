spring:
  application:
    name: ecommerce-service
  
  # Test Database Configuration
  datasource:
    url: jdbc:h2:mem:ecommerce_testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  # JPA Configuration for Tests
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
  
  # Flyway Configuration for Tests (Disabled)
  flyway:
    enabled: false

# Eureka Configuration for Tests (Disabled)
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

# Payment Provider Configuration for Tests (Mock)
payment:
  stripe:
    api-key: test_stripe_key
    webhook-secret: test_webhook_secret
  paypal:
    client-id: test_paypal_client_id
    client-secret: test_paypal_client_secret

# Actuator Configuration for Tests
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

# Logging Configuration for Tests
logging:
  level:
    com.raved.ecommerce: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# Test-specific properties
test:
  database:
    cleanup: true
  payment:
    mock: true
