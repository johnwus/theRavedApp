spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/raved_notifications
  jpa:
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587

logging:
  level:
    com.raved.notification: DEBUG
