auth:
  username: ${DATABASE_USERNAME}
  password: ${DATABASE_PASSWORD}
  database: raved_prod
primary:
  resources:
    requests:
      cpu: 1000m
      memory: 2Gi
    limits:
      cpu: 4000m
      memory: 8Gi
persistence:
  enabled: true
  size: 100Gi
service:
  ports:
    postgresql: 5432

