auth:
  username: raved
  password: ${RABBITMQ_PASSWORD}
replicaCount: 3
resources:
  requests:
    cpu: 300m
    memory: 1Gi
  limits:
    cpu: 1500m
    memory: 2Gi
persistence:
  enabled: true
  size: 20Gi
service:
  type: ClusterIP

