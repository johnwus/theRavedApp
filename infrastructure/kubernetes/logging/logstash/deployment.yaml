apiVersion: apps/v1
kind: Deployment
metadata:
  name: logstash
  namespace: raved-dev
spec:
  replicas: 1
  selector:
    matchLabels: { app: logstash }
  template:
    metadata:
      labels: { app: logstash }
    spec:
      containers:
        - name: logstash
          image: docker.elastic.co/logstash/logstash:8.12.2
          ports: [{ containerPort: 5044 }]

