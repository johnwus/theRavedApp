apiVersion: v1
kind: Service
metadata:
  name: realtime-service
  namespace: raved-dev
spec:
  selector: { app: realtime-service }
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
  type: ClusterIP

