apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-service
  namespace: raved-dev
spec:
  replicas: 1
  selector:
    matchLabels: { app: ecommerce-service }
  template:
    metadata:
      labels: { app: ecommerce-service }
    spec:
      containers:
        - name: ecommerce-service
          image: raved/ecommerce-service:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: common-config
            - configMapRef:
                name: ecommerce-service-config
            - secretRef:
                name: ecommerce-service-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 20
          resources:
            requests:
              cpu: 250m
              memory: 384Mi
            limits:
              cpu: 900m
              memory: 1Gi

