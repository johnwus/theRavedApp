apiVersion: v1
kind: Service
metadata:
  name: ecommerce-service
  namespace: raved-dev
spec:
  selector: { app: ecommerce-service }
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
  type: ClusterIP

