apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: raved-dev
spec:
  selector: { app: analytics-service }
  ports:
    - port: 8087
      targetPort: 8087
      protocol: TCP
  type: ClusterIP

